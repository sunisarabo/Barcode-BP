name: Deploy web app to GitHub Pages

# Deploy the web/ folder (the boarding pass reader PWA) to GitHub Pages.
# Pages serves over HTTPS, which is required for the camera scanner to work
# on phones. Trigger on pushes to the dev branch, or run manually.

on:
  push:
    branches:
      - claude/boarding-pass-reader-97tqfe
      - main
    paths:
      - "web/**"
      - ".github/workflows/deploy-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# อนุญาตให้ deploy ทีละงาน ไม่ทับกัน
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload web/ as Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: web

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
